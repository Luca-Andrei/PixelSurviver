#include "GameObserver.h"
#include <algorithm>
#include <memory>

// GameObserver is a pure virtual interface, so no implementation needed here
// The GameSubject methods are now implemented in GameSubject.h
